# Clé d'API LLM (clé produit — côté serveur uniquement, jamais commitée).
# Sans cette clé, la page /assistant répond 503.
LLM_API_KEY=

# Fournisseur compatible API OpenAI (défaut : https://api.openai.com/v1/chat/completions)
LLM_BASE_URL=

# Modèle à utiliser (défaut : gpt-4o-mini)
LLM_MODEL=